-# - binding.pry
.user-main
  .user-main__area
    = image_tag @user.avatar, class: :avatar
    - if @user.id == current_user.id
      %p.followText 詳細
    - elsif @boolean == true
      %p ooo
      -# = form_for @follow, url: :user_follows_path @user, method: "post" do |f|
      = form_for [@user, @follow], url: user_follows_path(@user) do |f|
        = f.submit "フォロー", class: "followText"
    - else
      = form_for [@user, @follow], url: user_follow_path(@user), method: "delete" do |f|
        = f.submit "フォロー中", class: "followText"

        

  .user-main__tweet
    = render partial: "tweet", collection: @tweets
  

  -# html: {method: "post"}) do |f|
